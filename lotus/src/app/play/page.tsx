import AIDraftSimulator from "../../components/AiDraftSimulator";

export default function PlayPage() {
    return (
        <main className="min-h-screen bg-lotus-bg">
            <AIDraftSimulator />
        </main>
    );
}